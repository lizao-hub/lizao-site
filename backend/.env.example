# 后端环境变量模板
# 复制这个文件为 .env，填入真实值。.env 不会被提交到 Git。

# 应用配置
APP_NAME=lizao-site
DEBUG=true

# 数据库（开发用 SQLite，生产换成 PostgreSQL）
DATABASE_URL=sqlite:///./lizao.db

# 允许跨域的前端地址（逗号分隔）
CORS_ORIGINS=http://localhost:5173

# 媒体文件存放目录
MEDIA_ROOT=./media
