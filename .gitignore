# ============================================
# 通用
# ============================================
.DS_Store
*.local
*.log

# 编辑器
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ============================================
# 前端 (frontend/)
# ============================================
node_modules/
frontend/dist/
frontend/dist-ssr/
# `npx vite build --outDir .verify-dist` 的验证产物（改完 CSS/排版跑一遍用的）
frontend/.verify-dist/
coverage/
*.tsbuildinfo
.eslintcache
frontend/__screenshots__/
*.timestamp-*-*.mjs

# 字体子集脚本的中间产物（npm run subset-font 会重新生成）
frontend/glyph-set.txt

# ============================================
# 后端 (backend/)
# ============================================
__pycache__/
*.py[cod]
*.so
.Python
build/
dist/
*.egg-info/
.venv/
venv/
env/

# 环境变量（含密钥，绝不提交）
.env
.env.*
!.env.example

# 数据库
*.sqlite3
*.db

# 上传的照片（本机开发用，线上走对象存储）
backend/media/

# 照片库（运行时数据，上传/裁剪后的照片落在这里，不属源码）
#
# ⚠️ **开头的斜杠不能删。** `data/` 不带锚点时匹配**任意层级**的 data 目录，
# 连 `frontend/src/data/`（那是源码：projects.ts / scenes.ts / guestbook.ts）
# 一起吞掉，而且**不报任何错** —— 新建的文件 git 直接当没看见。
# 2026-09-23 加留言页时踩到：新文件 `frontend/src/data/guestbook.ts` 没进
# 未跟踪列表，差一点就只提交了一份引用它的代码。
/data/

# ============================================
# 其他
# ============================================
.pytest_cache/
.mypy_cache/
.ruff_cache/
