# CONTEXT

本仓库的领域词汇表。命名（issue 标题、组件名、测试名、样式类名）以本文件为准。

## 这个站是什么

李泽敖的个人网站。定位是 **个人主页（personal home on the web）**：关于我、我写的东西、我拍的照片。

读者是**任何点进来的人**。招聘方也在其中，但不为招聘方做优化。「我的」是组织原则，不是「专业」。

前端 Vue 3 + Vite + TypeScript + Tailwind v3，纯静态产物交给 Nginx。后端 FastAPI，目前还没有 API。

## 视觉语言

风格取自《罗小黑战记》的视觉特征，拆解为色彩、线条、质感、动效、排版五个维度。目标是**新中式治愈系**，不是「可爱」也不是「二次元」。

### 术语

| 术语 | 含义 |
|---|---|
| **治愈系 / 新中式治愈系** | 整体调性。温暖、松弛、疏朗，带东方奇幻的日常感 |
| **手绘线条** | 非矢量完美的线条。允许轻微抖动、粗细变化、笔触质感。禁止绝对笔直、生硬的几何边框 |
| **手绘边框** | 预制的静态 SVG 路径组件（边框、分隔线、相框），复用手绘线条质感 |
| **自然意象** | 树叶、云朵、水滴、小鱼、猫爪印、藤蔓。作为背景纹理或分隔符，不是突兀的贴图 |
| **纸感** | 极淡的噪点 / 水彩渍 / 宣纸纹理叠加，消除数字化的冰冷感 |
| **疏朗通透** | 区块之间足够的呼吸空间，模仿动画里的空镜美学 |
| **灵动** | 微交互的物理感：轻微弹性形变、漂浮感、颜色晕染，Q 弹而非机械 |
| **色块偏移** | 用手绘排线或色块错位表现层次，不用真实投影，保持扁平化基调 |
| **点亮色** | 极少量用于按钮 / 链接悬停 / 重要高亮的暖色 |

### 色彩

- 底色**拒绝纯白**。米白、暖灰、淡茶色，模拟纸张或自然光。
- 主色取自原作经典配色：**墨色**（带暖灰的黑，不是纯黑）、**森林绿 / 苔藓绿**（低饱和灰绿）、**天空蓝 / 水色**（清透不刺眼）、**木质棕**（文字或边框）。
- 点缀色：极少量**暖黄**（灵力光点、灯笼）或**绯红**。
- 禁忌：高饱和荧光色、强烈黑白对比、大面积渐变。

### 排版

- 标题：手写体 / 圆体 / 有书法韵味的现代宋体。避免粗黑体、商务衬线体。
- 正文：细圆体或人文黑体，字重偏细，行高 1.6-1.8。
- 节奏错落有致，不严格对齐网格。允许倾斜和手写批注式排版，增强日记感与私密感。

### 明暗两套

手动的明暗切换，localStorage 记忆，首屏之前生效。暗色是**同一个世界的夜晚**（暖调墨夜），不是把变量反相的中性灰。

## 内容结构

| 路径 | 是什么 |
|---|---|
| `/` | **湖边**。整站的门，一幅画，点小木屋进屋里。没有导航 |
| `/room` | **屋里**。整站的目录，也就是「关于我」。点桌上的东西去各个页面 |
| `/projects`、`/projects/:slug` | 做过的东西。清单页尾部挂学历 / 论文 / 比赛 / 助教 |
| `/notes`、`/notes/:slug` | 笔记。Markdown 正文在 `src/content/notes/<slug>.md`，元信息在 `src/data/posts.ts` |
| `/gallery` | 影像。我的照片和朋友的照片 |

术语：**场景页**指 `/` 和 `/room` 这两个 100vh 不滚动、按百分比图层拼出来的水彩页面，
区别于「内容页」。场景的数据（图层坐标、可点区域、热点的去向）在 `src/data/scenes.ts`，
由 `SceneStage.vue` 渲染。见 `docs/adr/0008`、`docs/adr/0009`。

## 架构约定

- 颜色一律走 `src/assets/main.css` 里的 CSS 变量，Tailwind 只做映射。组件里不允许写死颜色值。
  （唯一例外：`index.html` 与 `useTheme.ts` 里的首屏底色，它们跑在 CSS 加载之前，用不了变量。）
- 中文字体走系统栈；只有标题字体是自托管的。
- `prefers-reduced-motion: reduce` 时必须关掉环境动效。
