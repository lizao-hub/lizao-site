/**
 * 身份信息。**站上只有这一份，改名字 / 邮箱 / GitHub 只改这里。**
 *
 * 用到它的地方一共五处：
 *   router/index.ts  站点名（SITE_NAME，拼每页 title）
 *   HomeView.vue     藏在景下面的 <h1>，以及湖边那行碎念
 *   RoomView.vue     墙上那行联系方式（邮箱 + GitHub）
 *   SiteFooter.vue   页脚署名 + 签名 + 备案号
 */

/**
 * 公开的身份信息。**只放真的会被渲染出来的字段。**
 */
export const profile = {
  name: 'LIzao 的自留地',
  /** 一句话定位。首页那块字下面的 <h1> 用它 */
  role: '数学硕士研究生 · 工业时序大模型 / AI Agent',
  /**
   * 签名。**只是一句碎念，不是简历。**
   *
   * 它同时活在两处：页脚最上面一行（每一页，湖边除外），
   * 和湖边那三行景里的第二行。**同一句话只写在这里** ——
   * 曾经两处各写一份，改一处忘一处（这行和 `role` 一样，
   * 不是「文案」而是「站上会被渲染出来的事实」）。
   */
  signature: '你还好吗请问我不是很好',
  github: 'https://github.com/lizao-hub',
  githubLabel: '@lizao-hub',
  /** 邮箱。屋里页露出，是站上唯一的联系方式（电话不展示） */
  email: '2443642197@qq.com',
} as const

/**
 * 备案信息。**只有页脚用**，写在这里是因为它和 `profile` 一样属于
 * 「站上会被渲染出来的事实」——换主机、换备案主体时要改的就是这一处。
 *
 * 两条规矩，改之前先看清楚：
 *
 * 1. `number` **必须原样展示**。「浙ICP备2026078754号」里那个省份简称和
 *    末尾的序号是备案系统分配的，少一个字、把「浙」写成「浙江」都不算合规。
 *    **不要给它加空格、不要拆行、不要为了好看改字号分级。**
 * 2. `href` 必须链到工信部备案系统（`https://beian.miit.gov.cn/`），
 *    **不是链到本站、也不是链到某个第三方查询页**。这是备案要求的固定动作，
 *    页脚那个 `<a>` 的 `target="_blank"` + `rel="noopener noreferrer"`
 *    就是为它配的，别删。
 *
 * 将来加了**公安联网备案**（形如「沪公网安备 31010402000000号」，
 * 链到 `https://beian.mps.gov.cn/`）再往这里加第二个字段 —— 它的展示规矩
 * 和 ICP 一样：原样、成对（号 + 链），不拆。
 */
export const beian = {
  number: '浙ICP备2026078754号',
  href: 'https://beian.miit.gov.cn/',
} as const
